---
platform: linux

image_resource:
  type: docker-image
  source: { repository: concourse/bosh-cli }

inputs:
  - name: resource-gambitcardddb

outputs:
  - name: updated-gambit

run:
  path: GambitCardDDB/list_files.sh
